#include <stdio.h>

int main(){
    
   float a, b, c, d, soma;
   
   printf("digite");
   scanf("%f %f %f %f", &a, &b, &c, &d);
   
   soma= (a+b+c+d)/4;
   
   printf("média é: %f", soma);
       
    return 0;
}
